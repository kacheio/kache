apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kache.fullname" . }}-test-connection"
  labels:
    {{- include "kache.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kache.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
